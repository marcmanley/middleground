node_modules/
temporary screenshots/
.DS_Store
.env
.env.*

# Raw, unoptimized source photos (originals can be multi-MB each).
# The site only ever references the optimized copies in media/images/web/.

# removed these for now.

# media/images/*.png
# media/images/*.jpg
# media/images/*.jpeg
